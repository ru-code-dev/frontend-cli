node_modules
dist
.DS_Store

# flow
.claude/
WORKFLOW/
pnpm-lock.yaml
ru-code-packages
.pnpmfile.cjs

# the release artifact: `pnpm release` writes it, the owner commits it on a release thread
/fg.mjs
