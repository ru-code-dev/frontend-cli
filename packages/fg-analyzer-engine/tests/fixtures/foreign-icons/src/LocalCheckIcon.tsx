// The project's own icon: not an import from an icon library, so nothing to report.
export function LocalCheckIcon() {
  return (
    <svg viewBox="0 0 16 16" width="16" height="16" aria-hidden="true" focusable="false">
      <path d="M3 8 L7 12 L13 4" />
    </svg>
  );
}
